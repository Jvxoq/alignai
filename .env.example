# Copy to .env and fill in values:
#   cp .env.example .env
#
# Note: backend/.env, agent/.env, frontend/.env are separate
# for running services on the host (outside Docker).

GROQ_API_KEY=
GEMINI_API_KEY=
# Qdrant Cloud — the agent connects here (no local Qdrant container)
QDRANT_URL=
QDRANT_API_KEY=
